# 📦 chat RESTful API (MongoDB + Express)

A fully RESTful chat API built with **Node.js**, **Express**, **MongoDB**, and **EJS**. This project allows creating, reading, updating, and deleting chat messages via HTTP methods.
